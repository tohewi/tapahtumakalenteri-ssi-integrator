{
  "name": "ssi-tools",
  "version": "1.0.0",
  "private": true,
  "description": "SSI Tools monorepo - scoring and match management platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
